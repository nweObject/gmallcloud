<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall.manage.mapper.PmsSkuInfoMapper">
    <select id="seleSkuInfoBySpuId" resultMap="seleSkuInfoBySpuIdMap">
        select psi.*, psi.id as psi_id, ssav.*, ssav.id as ssav_id
        from pms_sku_info psi JOIN pms_sku_sale_attr_value ssav
        on psi.id = ssav.sku_id and psi.product_id = #{spuId};
    </select>
    <resultMap id="seleSkuInfoBySpuIdMap" type="com.atguigu.gmall.bean.PmsSkuInfo" autoMapping="true">
        <result column="psi_id" property="id"></result>
        <collection property="skuSaleAttrValueList" ofType="com.atguigu.gmall.bean.PmsSkuSaleAttrValue" autoMapping="true">
            <result column="ssav_id" property="id"></result>
        </collection>

    </resultMap>
</mapper>